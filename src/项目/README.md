# src/项目/

当前主版本 v4.3.7 的源代码。

## 应该放什么

- `chinese-bank.js` / `chinese-manual.js`
- `math-bank.js` / `math-manual.js`
- 其余当前主版本的 JS / 配置文件

## 上传方式

将对应的 `.js` / `.zip` 文件直接 commit 即可。如果是 zip 形式的历史归档，请放到 `archive/备份/` 而不是这里。
