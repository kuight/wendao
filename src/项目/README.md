# src/项目

本目录归位的是 **原档合并.zip** 内嵌 `项目/` 目录中的真实源文件，不是二次改写后的重建版本。

## 归属说明

- `问道修仙学院-v2.2-phase1-bugfix` 属于**修真分支首发可考节点**。
- 当前归位到本目录的 `chinese/` 与 `math/` 文件，是从该批原始项目材料中直接提取出来的**修真分支首批语文 / 数学题库与讲义源码**。
- `杂项分支.zip` 保持原始压缩状态归档，不拆散、不重排；若其中包含 `auto-visualizer.js`、`immersive-learning.js`、`pixel-art.js`、`side-dock.js` 等修真资产，则只在这里说明其为**修真资产完整快照**，本次不改动其内部位置。

## 目录说明

- `chinese/`：修真分支语文题库与讲义源码
- `math/`：修真分支数学题库与讲义源码
- `杂项分支.zip`：原始分包，保留为修真 `v4.1-hotfix` 一系资产快照入口

## 原始性原则

本目录只做“归位”，不做语义改写：

- 不新增 `src/项目/项目/` 之类双重嵌套
- 不拆解 `杂项分支.zip`
- 不改写原始 `.js` 内容
- 后续若需要细分更多学科，应继续从原始归档中提取后再补充
