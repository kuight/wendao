# thunder/

雷劫限时闯关整套逻辑在原始 `wendao4.4.0.zip` 中以**单一 JS 文件**存在，**未找到多个独立的 thunder_*.js 文件**。

```
assets/thunder-tribulation-v440.js    # 雷劫闯关主引擎 (含 3 档模式)
```

为保留完整源码，已将该单文件落地到 `../v440-core/thunder-tribulation-v440.js`。此处空目录是**诚实的"未找到(独立文件)"**标记。
