# chem-xbx/

化学 12 篇手写小秘籍在原始 `wendao4.4.0.zip` 中以**单一 JS 文件**形式存在，**未找到 12 个独立的 chem_xbx_*.js 文件**。

```
data/chemistry-xbx-manual-v440.js    # 12 篇合一手写秘籍 (12.31 KB)
data/deep-dive-chemistry-v440.js     # 12 篇合一手写讲义 (81.78 KB)
```

key 名（`chem_xbx_lizi`、`chem_xbx_redox` 等）作为 `CHEMISTRY_MANUALS` 数组元素挂在 `chemistry-xbx-manual-v440.js` 里。

为保留化学讲义完整源码，已将这两个合成文件落地到 `../v440-core/`。此处空目录是**诚实的"未找到(独立文件)"**标记 — 12 篇内容存在，但不在 12 个独立 JS 文件中。
