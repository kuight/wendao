# Phase 14 Bug Hunt Report (2026-08-19)

Generated from local analysis of `v4.5.0-fresh` asset (`wendao4.4.0-playable.zip`, SHA-256 `19715861f4d5eabf930210e1b6fc0a2670769cdb4330172caf2cec3b35d44cc2`) against the current `main` HEAD `f37e1ff5f96cc20b077e9934758072bc93af6efa`.

## Bug 1.1 — JS hard-blocker — PR #32 (open)

| Field | Value |
|---|---|
| File | `src/assets/battle-arena-v437-balance.js` |
| Line | 239 |
| Symptom | `SyntaxError: Unexpected token '%'` |
| Root cause | `每回合掉血%: …` — `%` invalid in IdentifierPart |
| Effect | Entire IIFE fails to parse → `window.v437balance` never set → v4.3.7-balance patch silently no-ops |
| Fix | quote the property key: `'每回合掉血%': …` |
| Verified | static patch review completed |

## Bug 1.2 — MIME mismatch — deferred

| Field | Value |
|---|---|
| Path | `src/assets/img/emotes/{angry,deadpan,love,peek,plead,smug,tea,thumbsup,work}.png` (9 files) |
| Symptom | File magic bytes are JPEG SOI (`FF D8 FF`) but extension is `.png` |
| Detected by | Phase 14 bug hunter C6 |
| Phase 6 status | Earlier PNG→JPG rename campaign converted README/banner assets; emotes subdir not touched |
| Why deferred | Extension rename requires coordinated reference updates and cache-safe rollout |
| Suggested plan | add `.jpg`, update refs, then remove old `.png` in a dedicated follow-up chain |

## Other checks — all green

| Check | Result |
|---|---|
| C1 JS parse sweep | 1 fail = bug 1.1 |
| C2 HTML refs | 7 HTML files, 0 broken `<script>/<link>/<img>` |
| C3 JS internal refs | 0 missing fetch/import/getScript targets |
| C4 MANIFEST.sha256 | strict format and hash/size checks passed |
| C5 LFS pointers | 0 in shipping zip |
| C6 MIME mismatch | only deferred bug 1.2 set |
| C8 README consistency | subject counts and timeline crosscheck OK |

## Next step queue

1. Merge PR #32 — restores v4.3.7-balance.
2. Then open the bug 1.2 rename/reference PR chain.
